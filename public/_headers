# Cloudflare Workers Static Assets（见 wrangler.jsonc）默认对所有资源发
# `Cache-Control: public, max-age=0, must-revalidate`，所以连带内容哈希的构建产物
# 也会在每次导航时回源校验一遍，浏览器磁盘缓存等于白放。
# /_astro/ 下的文件名自带内容哈希，同一 URL 的内容永不改变，可以安全长期固定。
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# 博客 OG 卡片每次构建重新生成但 URL 不变（/og/<slug>.png），所以只给一天浏览器缓存，
# 不能用 immutable，否则改了标题分享图会长期不更新。
/og/*
  Cache-Control: public, max-age=86400

# HTML 保持默认的 must-revalidate：部署后要立刻可见，不在此覆盖。
